# Placeholder for AWS/EC2 deployment. Real production overrides (image tags, secrets,
# S3/attachment config, reverse proxy) land in Phase 8 — see PLAN.md §8. For now this
# proves the same service set builds under an alternate compose file.
services:
  enrollment-service:
    build: ../services/enrollment-service
    restart: unless-stopped

  claims-intake-service:
    build: ../services/claims-intake-service
    restart: unless-stopped

  adjudication-service:
    build: ../services/adjudication-service
    restart: unless-stopped

  notification-service:
    build: ../services/notification-service
    restart: unless-stopped

  rag-assistant-service:
    build: ../services/rag-assistant-service
    restart: unless-stopped

  frontend:
    build: ../frontend
    restart: unless-stopped
